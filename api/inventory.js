const {sb}=require('../lib/server');module.exports=async function handler(req,res){if(req.method!=='GET')return res.status(405).json({ok:false,error:'Method not allowed'});try{await sb('rpc/release_expired_reservations',{method:'POST',body:'{}'}).catch(()=>null);const rows=await sb('inventory?select=id,handle,platform,kind,sale_price&status=eq.available&order=sale_price.asc',{method:'GET'});res.setHeader('Cache-Control','no-store');return res.status(200).json({ok:true,items:rows})}catch(e){return res.status(500).json({ok:false,error:'Inventory is temporarily unavailable.'})}}