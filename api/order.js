const {sb,telegram,sendEmail,html,methodLabel,env}=require('../lib/server');
const validMethods=new Set(['cashapp','zelle','paypal','crypto']);
function orderKeyboard(id){const base=(env('SITE_URL',false)||'https://mikyemedia.com').replace(/\/$/,'');return {inline_keyboard:[[{text:'💰 Mark paid',callback_data:`paid:${id}`},{text:'🔄 Refresh',callback_data:`refresh:${id}`}],[{text:'📦 Mark secured',callback_data:`secured:${id}`},{text:'❌ Release',callback_data:`releaseask:${id}`}],[{text:'✉️ Open fulfillment',url:`${base}/admin.html?order=${encodeURIComponent(id)}`}]]}}
module.exports=async function handler(req,res){if(req.method!=='POST')return res.status(405).json({ok:false,error:'Method not allowed'});try{const b=req.body||{};const inventoryId=Number(b.inventoryId);const email=String(b.email||'').trim().toLowerCase();const paymentMethod=String(b.paymentMethod||'');const payerName=String(b.payerName||'').trim();const paymentRef=String(b.paymentRef||'').trim();const note=String(b.note||'').trim();if(!inventoryId||!/^\S+@\S+\.\S+$/.test(email)||!validMethods.has(paymentMethod)||payerName.length<2||paymentRef.length<2)return res.status(400).json({ok:false,error:'Please complete all required checkout fields.'});if(note.length>500||payerName.length>100||paymentRef.length>160)return res.status(400).json({ok:false,error:'One or more fields are too long.'});
const out=await sb('rpc/create_order',{method:'POST',headers:{Prefer:'return=representation'},body:JSON.stringify({p_inventory_id:inventoryId,p_buyer_email:email,p_payment_method:paymentMethod,p_payer_name:payerName,p_payment_ref:paymentRef,p_buyer_note:note})});const o=Array.isArray(out)?out[0]:out;if(!o)throw new Error('Could not create order');const profit=Number(o.sale_price)-Number(o.cost);const alert=`🛒 <b>NEW HANDLE ORDER</b>\n\n<b>Order:</b> ${html(o.public_id)}\n<b>Handle:</b> ${html(o.handle)} (${html(o.platform)} ${html(o.kind)})\n<b>Supplier:</b> ${html(o.source)}\n<b>Your cost:</b> $${Number(o.cost).toFixed(0)}\n<b>Sale:</b> $${Number(o.sale_price).toFixed(0)}\n<b>Gross profit:</b> $${profit.toFixed(0)}\n\n<b>Payment:</b> ${html(methodLabel(paymentMethod))}\n<b>Sender:</b> ${html(payerName)}\n<b>Reference:</b> ${html(paymentRef)}\n<b>Buyer:</b> ${html(email)}${note?`\n<b>Note:</b> ${html(note)}`:''}\n\n⏳ <b>Stage:</b> Awaiting payment verification\n⚠️ Check the payment independently before pressing Mark paid.`;await telegram(alert,{reply_markup:orderKeyboard(o.public_id)}).catch(()=>false);await sendEmail({to:email,subject:`Order ${o.public_id} received — ${o.handle}`,htmlBody:`<div style="font-family:Arial,sans-serif;max-width:560px;margin:auto;color:#111"><h2>Order received</h2><p>Your order for <b>${html(o.handle)}</b> has been submitted.</p><p><b>Order ID:</b> ${html(o.public_id)}<br><b>Amount:</b> $${Number(o.sale_price).toFixed(0)}<br><b>Status:</b> Awaiting payment verification</p><p>This email does not mean payment has been approved. The payment will be checked manually before fulfillment.</p><p style="color:#666;font-size:12px">Mikye Media · mikyemedia.com</p></div>`,textBody:`Mikye Media\n\nOrder received\n\nHandle: ${o.handle}\nOrder ID: ${o.public_id}\nAmount: $${Number(o.sale_price).toFixed(0)}\nStatus: Awaiting payment verification\n\nThis email does not mean payment has been approved. Payment will be checked manually before fulfillment.\n\nmikyemedia.com`}).catch(()=>null);return res.status(200).json({ok:true,orderId:o.public_id,reservedUntil:o.reserved_until})}catch(e){const msg=String(e.message||'');if(msg.includes('ITEM_NOT_AVAILABLE'))return res.status(409).json({ok:false,error:'That handle is no longer available.'});return res.status(500).json({ok:false,error:'Could not create the order. Please try again.'})}}